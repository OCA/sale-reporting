<openerp>
  <data>
    <record model="ir.ui.view" id="sale_order_form_add_condition">
      <field name="name">condition_template.sale.order.form</field>
      <field name="model">sale.order</field>
      <field name="inherit_id" ref="sale.view_order_form"/>
      <field name="arch" type="xml">
        <xpath expr="//page[@string='Other Information']" position="after">
          <page string="Conditions" name="conditions">
            <label string="The conditions will be displayed on the printed document. You can load a predefined template, write your own text or load a template and then modify it only for this document." colspan="2"/>
            <group string="Top Conditions">
                <field name="condition_template1_id"
                  string="Load a template"
                  domain="[('position','=','before_lines')]"
                  context="{'default_position': 'before_lines'}"
                  on_change="set_note1(condition_template1_id, partner_id)"/>
              <field name="note1" nolabel="1" colspan="2"/>
            </group>
            <group string="Bottom Conditions">
              <field name="condition_template2_id"
                string="Load a template"
                context="{'default_position': 'after_lines'}"
                domain="[('position','=','after_lines')]"
                on_change="set_note2(condition_template2_id, partner_id)"/>
              <field name="note2" nolabel="1" colspan="2"/>
            </group>
          </page>
        </xpath>
      </field>
    </record>
  </data>
</openerp>
